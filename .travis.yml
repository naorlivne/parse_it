language: python
dist: xenial
python:
  - '3.7'
services:
  - docker
install:
  - pip install -r requirements.txt
script:
  - python -m unittest discover
deploy:
  provider: pypi
  user: naorlivne
  password:
    secure: need_to_generate_a_new_one_for_this_repo
  distributions: sdist bdist_wheel
  skip_existing: true
